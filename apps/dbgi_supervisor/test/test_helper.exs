# ExUnit framework start for the dbgi_supervisor app.
# Sub-phase 7 [BEAM-DBGI-APP-SKELETON]: minimal start.
# Substantive Sandbox config forward-queued to sub-phase 8+ when
# Repo access lands at substantive substrate register per ADR-0028 §3
# canonical (DBGI may reuse `CosmpRouter.Repo` per Q-RQ1 mix umbrella
# shared-Repo canonical OR introduce dedicated `DbgiSupervisor.Repo`
# at sub-phase 8 forward-queue scope; substrate-architectural decision
# at sub-phase 8 Phase 0 RULE 11 surface register).
ExUnit.start()
