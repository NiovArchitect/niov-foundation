{
  "name": "@niov/database",
  "version": "0.0.1",
  "private": true,
  "description": "NIOV Foundation -- Prisma schema, database client, and audited query functions.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./client": "./src/client.ts",
    "./audit": "./src/audit.ts",
    "./queries/entity": "./src/queries/entity.ts"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@niov/auth": "*",
    "@prisma/client": "^6.1.0"
  },
  "devDependencies": {
    "prisma": "^6.1.0"
  }
}
